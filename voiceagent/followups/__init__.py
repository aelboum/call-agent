"""`voiceagent.followups` -- the Call Outcome & Follow-up foundation (Phase
2.7).

`CallOutcome` is the business result of a call, distinct from
`CallSession.status`'s technical lifecycle. `FollowUpAction` is one
explicit action resulting from a call -- not a generic task table, not a
workflow engine (brief §2/§26).
"""

from __future__ import annotations
