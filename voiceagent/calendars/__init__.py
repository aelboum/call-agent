"""`voiceagent.calendars` -- the minimal internal calendar foundation (Phase
2.6). Exists so a call agent can check availability, schedule an
appointment, and cancel one -- not a general calendar application (brief
§5): no recurrence, no attendees, no external provider synchronization.
"""

from __future__ import annotations
